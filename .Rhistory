"C:/Users/LENOVO/AppData/Local/Programs/Python/Python311/python.exe",
required = TRUE
)
reticulate::use_python(
"C:/Users/LENOVO/AppData/Local/Programs/Python/Python311/python.exe",
required = TRUE
)
reticulate::virtualenv_create(
envname = "r-transformers-311",
python  = "C:/Users/LENOVO/AppData/Local/Programs/Python/Python311/python.exe"
)
reticulate::use_virtualenv("r-transformers-311", required = TRUE)
reticulate::py_install(
packages = c("transformers", "sentencepiece"),
envname  = "r-transformers-311",
pip      = TRUE
)
# PASO 5: Importar y usar
transformers <- reticulate::import("transformers")
classifier <- transformers$pipeline(
task  = "text-classification",
model = "pysentimiento/robertuito-sentiment-analysis"
)
# Paso 1: Instalar torch manualmente en el entorno correcto
reticulate::py_run_string("
import subprocess, sys
subprocess.check_call([
sys.executable, '-m', 'pip', 'install',
'torch==2.2.2',
'--index-url', 'https://download.pytorch.org/whl/cpu'
])
")
# PASO 5: Importar y usar
transformers <- reticulate::import("transformers")
classifier <- transformers$pipeline(
task  = "text-classification",
model = "pysentimiento/robertuito-sentiment-analysis"
)
# Crear entorno conda con Python 3.11
reticulate::conda_create(
envname  = "r-torch",
packages = "python=3.11"
)
reticulate::use_condaenv("r-torch", required = TRUE)
# Crear entorno conda con Python 3.11
reticulate::conda_create(
envname  = "r-torch",
packages = "python=3.11",
conda    = "C:/Users/LENOVO/miniconda3/Scripts/conda.exe"
)
# Crear entorno conda con Python 3.11
reticulate::conda_create(
envname  = "r-torch",
packages = "python=3.11",
conda    = "C:/Users/LENOVO/miniconda3/Scripts/conda.exe"
)
reticulate::use_condaenv("r-torch", required = TRUE)
reticulate::use_condaenv(
condaenv = "C:/Users/LENOVO/miniconda3/envs/r-torch",
required = TRUE
)
reticulate::conda_install(
envname  = "r-torch",
packages = c("pytorch==2.4.0", "cpuonly"),
channel  = "pytorch",
conda    = "C:/Users/LENOVO/miniconda3/Scripts/conda.exe"
)
reticulate::conda_install(
envname  = "r-torch",
packages = c("transformers==4.40.0", "sentencepiece"),
pip      = TRUE,
conda    = "C:/Users/LENOVO/miniconda3/Scripts/conda.exe"
)
reticulate::py_run_string("import torch; print(torch.__version__)")
transformers <- reticulate::import("transformers")
classifier <- transformers$pipeline(
task  = "text-classification",
model = "pysentimiento/robertuito-sentiment-analysis"
)
resultado <- classifier("Me encanta este producto")
print(resultado)
menciones_conyuge <- toks_sentencia %>%
kwic( pattern = "cónyuge",  window = 12)
#install.packages("pdftools")
pacman::p_load(pdftools, stringr, quanteda, quanteda.textstats, quanteda.textplots, dplyr, ggplot2, reticulate)
menciones_conyuge <- toks_sentencia %>%
kwic( pattern = "cónyuge",  window = 12)
# Armar dataframe con los primeros 10 extractos
data <- data.frame(pre  = menciones_conyuge$pre, post = menciones_conyuge$post ) %>%
slice(1:10) %>%
mutate(text = paste(pre, post)) %>%
select(text)
# Predecir sentimiento para cada uno de los textos
out <-  map(data$text, classifier)
#install.packages("pdftools")
pacman::p_load(pdftools, stringr, quanteda, quanteda.textstats, quanteda.textplots, dplyr, ggplot2, reticulate, purrr)
# Predecir sentimiento para cada uno de los textos
out <-  map(data$text, classifier)
# Extraer la etiqueta de cada predicción
labels <- flatten(out) %>%
map_chr(1)
# Extraer probabilidad de cada predicción
prob <- flatten(out) %>%
map_dbl(2)
# Añadir columnas a data frame
data <- data %>%
mutate(sentiment = labels,
prob = prob)
View(data)
View(menciones_conyuge)
# Armar dataframe con los primeros 10 extractos
data <- data.frame(pre  = menciones_conyuge$pre, post = menciones_conyuge$post ) %>%
slice(1:67) %>%
mutate(text = paste(pre, post)) %>%
select(text)
# Predecir sentimiento para cada uno de los textos
out <-  map(data$text, classifier)
# Extraer la etiqueta de cada predicción
labels <- flatten(out) %>%
map_chr(1)
# Extraer probabilidad de cada predicción
prob <- flatten(out) %>%
map_dbl(2)
# Añadir columnas a data frame
data <- data %>%
mutate(sentiment = labels,
prob = prob)
View(data)
data %>%
kbl() %>%
kable_styling(font_size = 20)
#install.packages("pdftools")
pacman::p_load(pdftools, stringr, quanteda, quanteda.textstats, quanteda.textplots, dplyr, ggplot2, reticulate, purrr, knitr)
data %>%
kbl() %>%
kable_styling(font_size = 20)
#install.packages("pdftools")
pacman::p_load(pdftools, stringr, quanteda, quanteda.textstats, quanteda.textplots, dplyr, ggplot2, reticulate, purrr, knitr, kableExra)
data %>%
kbl() %>%
kable_styling(font_size = 20)
data %>%
kbl() %>%
kableExtra::kable_styling(font_size = 20)
data %>%
kableExtra::kbl() %>%
kableExtra::kable_styling(font_size = 20)
reticulate::py_run_string("import torch; print(torch.__version__)")
menciones_infidelidad <- toks_sentencia %>%
kwic( pattern = "infidelidad",  window = 12)
menciones_infidelidad <- toks_sentencia %>%
kwic( pattern = "infidelidad",  window = 12)
# Armar dataframe con los primeros 10 extractos
data1 <- data.frame(pre  = menciones_infidelidad$pre, post = menciones_infidelidad$post ) %>%
slice(1:48) %>%
mutate(text = paste(pre, post)) %>%
select(text)
# Predecir sentimiento para cada uno de los textos
out1 <-  map(data1$text, classifier)
# Predecir sentimiento para cada uno de los textos
out1 <-  map(data1$text, classifier)
# Extraer la etiqueta de cada predicción
labels1 <- flatten(out1) %>%
map_chr(1)
# Extraer probabilidad de cada predicción
prob1 <- flatten(out1) %>%
map_dbl(2)
# Añadir columnas a data frame
data1 <- data1 %>%
mutate(sentiment = labels1,
prob = prob1)
data1 %>%
kableExtra::kbl() %>%
kableExtra::kable_styling(font_size = 20)
library(word2vec)
install.packages("word2vec")
library(word2vec)
x <- data$cuerpo # noticias
# Generar embeddings de 300 dimensiones
model <- word2vec(x = x, dim = 300, iter = 20, min_count = 5, threads = 16)
View(data)
View(psentencia)
#install.packages("pdftools")
pacman::p_load(pdftools, stringr, quanteda, quanteda.textstats, quanteda.textplots, dplyr, ggplot2, reticulate, purrr, knitr, kableExra)
#install.packages("pdftools")
pacman::p_load(pdftools, stringr, quanteda, quanteda.textstats, quanteda.textplots, dplyr, ggplot2, reticulate, purrr, knitr, kableExra)
texto <- pdf_text("../input/sentencia.pdf")
texto <- str_to_lower(texto)
data <- data.frame(texto)
View(data)
x <- data# noticias
# Generar embeddings de 300 dimensiones
model <- word2vec(x = x, dim = 300, iter = 20, min_count = 5, threads = 16)
# Esto es un listado de oraciones
oraciones_editadas <- map(corpus_sentecia, ~split_and_edit(.x ) )  %>%
flatten()  # desanclarar oraciones de la noticia
vector_representation_sample <- readRDS("~/GitHub/analisis-inicial/input/vector_representation_sample.rds")
View(vector_representation_sample)
## Aca ejemplo
# Eliminar textos sin representación y con menos de 4 palabras
representations2 <- representations %>%
keep(~!is_empty(.x[[1]])) %>% # sacar las oraciones sin representación
keep(~.x[3] > 4) # sacar textos con pocas palabras
representations <- vector_representation_sample
# Agregar oraciones originales a la lista
representations <- map2(representations, oraciones_muestra_originales , ~append(.x, .y))
## Aca ejemplo
# Eliminar textos sin representación y con menos de 4 palabras
representations2 <- representations %>%
keep(~!is_empty(.x[[1]])) %>% # sacar las oraciones sin representación
keep(~.x[3] > 4) # sacar textos con pocas palabras
# Guardamos los vectores de todos los textos
vectores <- representations2 %>%
map(1) %>%
map(~unlist(unname(.x)))
# Guardamos las oraciones editadas
textos <- representations2 %>%
map(2)
# Guardamos las oraciones editadas
textos_originales <- representations2 %>%
map(4)
function(vectores, texto, n = 3, lista_textos ) {
vector <-  create_representation(texto) # ya la conocemos
vector <- vector[[1]] %>% unname() %>% unlist() # simplificar lista
similitud <-  map_dbl(vectores, ~coop::cosine(vector, .x ) ) # comparar cada vector contra todos los demás
top <- order(similitud, decreasing=T)[1:n] # ordenar de mayor a menor similitud
return(lista_textos[top])
}
encontrar_mas_parecidos(vectores, "elecciones congreso nacional", 5, lista_textos =  textos_originales)
function(vectores, texto, n = 3, lista_textos ) {
vector <-  create_representation(texto) # ya la conocemos
vector <- vector[[1]] %>% unname() %>% unlist() # simplificar lista
similitud <-  map_dbl(vectores, ~coop::cosine(vector, .x ) ) # comparar cada vector contra todos los demás
top <- order(similitud, decreasing=T)[1:n] # ordenar de mayor a menor similitud
return(lista_textos[top])
}
function(vectores, texto, n = 3, lista_textos ) {
vector <-  create_representation(texto) # ya la conocemos
vector <- vector[[1]] %>% unname() %>% unlist() # simplificar lista
similitud <-  map_dbl(vectores, ~coop::cosine(vector, .x ) ) # comparar cada vector contra todos los demás
top <- order(similitud, decreasing=T)[1:n] # ordenar de mayor a menor similitud
return(lista_textos[top])
}
View(vectores)
encontrar_mas_parecidos <-function(vectores, texto, n = 3, lista_textos ) {
vector <-  create_representation(texto) # ya la conocemos
vector <- vector[[1]] %>% unname() %>% unlist() # simplificar lista
similitud <-  map_dbl(vectores, ~coop::cosine(vector, .x ) ) # comparar cada vector contra todos los demás
top <- order(similitud, decreasing=T)[1:n] # ordenar de mayor a menor similitud
return(lista_textos[top])
}
encontrar_mas_parecidos(vectores, "elecciones congreso nacional", 5, lista_textos =  textos_originales)
create_representation <- function(texto) {
texto <-  oraciones_muestra[[1]]
var_names <- paste0("dim", 1:300)
doc = nlp(texto)
indices <- 0:(length(doc) - 1)
vectores <- map(indices,  ~doc[.x]$vector ) %>%
discard(function(x) sum(x) == 0 ) %>% # sacar las palabras no incluidas en el vocabulario
map(set_names, var_names) %>%
bind_rows()
representacion <-  map(vectores, mean)
return(list(representacion, texto, length(indices)) )
}
encontrar_mas_parecidos <-function(vectores, texto, n = 3, lista_textos ) {
vector <-  create_representation(texto) # ya la conocemos
vector <- vector[[1]] %>% unname() %>% unlist() # simplificar lista
similitud <-  map_dbl(vectores, ~coop::cosine(vector, .x ) ) # comparar cada vector contra todos los demás
top <- order(similitud, decreasing=T)[1:n] # ordenar de mayor a menor similitud
return(lista_textos[top])
}
encontrar_mas_parecidos(vectores, "elecciones congreso nacional", 5, lista_textos =  textos_originales)
# Agregar oraciones originales a la lista
representations <- map2(representations, oraciones_muestra_originales , ~append(.x, .y))
texto <- pdf_text("../input/sentencia.pdf")
texto <- str_to_lower(texto)
vectores <- map(indices,  ~doc[.x]$vector ) %>%
discard(function(x) sum(x) == 0 ) %>% # sacar las palabras no incluidas en el vocabulario
map(set_names, var_names) %>%
bind_rows()
library(word2vec)
x <- texto  # cada elemento = texto completo de una página
# Con páginas completas ya tienes suficiente texto para parámetros más robustos
model <- word2vec(
x         = x,
dim       = 100,       # 100 es razonable para un documento de 66 páginas
iter      = 30,        # suficiente para este tamaño
min_count = 2,         # palabras que aparecen al menos 2 veces en todo el doc
threads   = 4
)
# Guardar
write.word2vec(model, "modelo_documento.bin")
# Matriz de embeddings
embedding <- as.matrix(model)
# Verificar vocabulario resultante
cat("Palabras en vocabulario:", nrow(embedding), "\n")
# Ver qué palabras quedaron en el vocabulario
vocab <- rownames(embedding)
head(vocab, 40)  # revisa esto primero para elegir palabras relevantes
# Buscar palabras cercanas con términos de TU documento
lookslike <- predict(
model,
newdata = c("infidelidad", "matrimonio", "familia", "cónyuge"),  # reemplaza con términos reales
type    = "nearest",
top_n   = 5
)
lookslike
var_names <- rownames(t(embedding))  # nombres de dimensiones
# Representación vectorial de cada página
vectores_paginas <- map(texto, function(pagina) {
palabras <- unlist(strsplit(tolower(pagina), "\\W+"))
palabras <- palabras[nchar(palabras) > 0]
vecs <- map(palabras, function(p) {
tryCatch(predict(model, p, type = "embedding"), error = function(e) NULL)
}) %>%
discard(is.null) %>%
discard(function(x) sum(x) == 0)
if (length(vecs) == 0) return(rep(0, 100))  # página sin palabras en vocabulario
# Promedio de todos los vectores = representación de la página
Reduce("+", vecs) / length(vecs)
})
# ✅ Generar nombres de dimensiones manualmente
var_names <- paste0("V", 1:ncol(embedding))  # genera "V1", "V2", ..., "V100"
# Representación vectorial de cada página
vectores_paginas <- map(texto, function(pagina) {
palabras <- unlist(strsplit(tolower(pagina), "\\W+"))
palabras <- palabras[nchar(palabras) > 0]
vecs <- map(palabras, function(p) {
tryCatch(predict(model, p, type = "embedding"), error = function(e) NULL)
}) %>%
discard(is.null) %>%
discard(function(x) sum(x) == 0)
if (length(vecs) == 0) return(rep(0, 100))  # página sin palabras en vocabulario
# Promedio de todos los vectores = representación de la página
Reduce("+", vecs) / length(vecs)
})
dim(embedding)
vectores_paginas <- map(texto, function(pagina) {
palabras <- unlist(strsplit(tolower(pagina), "\\W+"))
palabras <- palabras[nchar(palabras) > 0]
vecs <- map(palabras, function(p) {
tryCatch(predict(model, p, type = "embedding"), error = function(e) NULL)
}) %>%
discard(is.null) %>%
discard(function(x) {
v <- as.numeric(x)                    # convertir matriz → vector numérico
any(is.na(v)) || sum(v) == 0          # descartar si tiene NA o es vector cero
}) %>%
map(as.numeric)                         # asegurar que cada elemento sea vector
if (length(vecs) == 0) return(rep(0, 100))
# Promedio de vectores = representación de la página
Reduce("+", vecs) / length(vecs)
})
View(vectores_paginas)
# Buscar las 3 páginas más relevantes para una consulta
encontrar_paginas("infidelidad", n = 3)
encontrar_paginas <- function(consulta, n = 3) {
vec_consulta <- tryCatch(
predict(model, unlist(strsplit(tolower(consulta), "\\W+")), type = "embedding"),
error = function(e) NULL
)
if (is.null(vec_consulta)) {
message("Ninguna palabra de la consulta está en el vocabulario")
return(NULL)
}
vec_consulta <- colMeans(vec_consulta)
similitudes <- map_dbl(vectores_paginas, ~coop::cosine(vec_consulta, .x))
top <- order(similitudes, decreasing = TRUE)[1:n]
# Mostrar número de página y similitud
cat("Páginas más relevantes:\n")
for (i in seq_along(top)) {
cat(sprintf("  Página %d — similitud: %.3f\n", top[i], similitudes[top[i]]))
}
return(invisible(texto[top]))
}
#
# Buscar las 3 páginas más relevantes para una consulta
encontrar_paginas("infidelidad", n = 3)
install.packages("coop")
#
# Buscar las 3 páginas más relevantes para una consulta
encontrar_paginas("infidelidad", n = 3)
install.packages(c("tidytext", "topicmodels", "ldatuning", "dplyr", "tidyr"))
library(tidytext)
library(topicmodels)
install.packages("topicmodels")
library(topicmodels)
library(ldatuning)
library(dplyr)
library(tidyr)
library(purrr)
# Dividir cada página en frases
frases <- map2_dfr(texto, seq_along(texto), function(pagina, num_pagina) {
fs <- unlist(strsplit(pagina, "(?<=[.!?])\\s+", perl = TRUE))
fs <- fs[nchar(trimws(fs)) > 20]  # descartar frases muy cortas
tibble(pagina = num_pagina, frase_id = seq_along(fs), texto = fs)
})
cat("Total de frases extraídas:", nrow(frases), "\n")
# Tokenizar y eliminar stopwords en español
data("stop_words")  # stopwords en inglés — complementar con español abajo
stopwords_es <- c("de", "la", "el", "en", "y", "a", "los", "las", "un", "una",
"que", "se", "del", "por", "con", "no", "es", "su", "al",
"lo", "como", "más", "pero", "sus", "le", "ya", "o", "fue",
"este", "ha", "si", "porque", "esta", "son", "entre", "cuando",
"muy", "sin", "sobre", "también", "me", "hasta", "hay", "donde",
"quien", "desde", "todo", "nos", "durante", "uno", "ni", "contra")
dtm <- frases %>%
unnest_tokens(palabra, texto) %>%
filter(!palabra %in% stopwords_es) %>%
filter(nchar(palabra) > 3) %>%          # descartar palabras muy cortas
count(frase_id, palabra) %>%
cast_dtm(frase_id, palabra, n)
cat("Dimensiones DTM:", dim(dtm), "\n")  # frases x palabras
library(tidytext)
install.packages("tidytext")
library(tidytext)
library(ldatuning)
install.packages("ldatuning")
library(ldatuning)
dtm <- frases %>%
unnest_tokens(palabra, texto) %>%
filter(!palabra %in% stopwords_es) %>%
filter(nchar(palabra) > 3) %>%          # descartar palabras muy cortas
count(frase_id, palabra) %>%
cast_dtm(frase_id, palabra, n)
cat("Dimensiones DTM:", dim(dtm), "\n")  # frases x palabras
# Probar entre 2 y 15 tópicos y medir cuál es mejor
resultado_k <- FindTopicsNumber(
dtm,
topics   = seq(2, 15, by = 1),
metrics  = c("Griffiths2004", "CaoJuan2009", "Arun2010", "Deveaud2014"),
method   = "Gibbs",
control  = list(seed = 42),
verbose  = TRUE
)
library(ldatuning)
install.packages("ldatuning")
library(ldatuning)
# Probar entre 2 y 15 tópicos y medir cuál es mejor
resultado_k <- FindTopicsNumber(
dtm,
topics   = seq(2, 15, by = 1),
metrics  = c("Griffiths2004", "CaoJuan2009", "Arun2010", "Deveaud2014"),
method   = "Gibbs",
control  = list(seed = 42),
verbose  = TRUE
)
# Visualizar — busca donde las métricas se estabilizan
FindTopicsNumber_plot(resultado_k)
library(topicmodels)
# Probar distintos valores de k
valores_k <- 2:10
perplejidad <- map_dbl(valores_k, function(k) {
modelo <- LDA(dtm, k = k, method = "Gibbs",
control = list(seed = 42, iter = 500, burnin = 100))
perplexity(modelo)
})
perplejidad <- map_dbl(valores_k, function(k) {
modelo <- LDA(dtm, k = k, method = "Gibbs",
control = list(seed = 42, iter = 500, burnin = 100))
perplexity(modelo, newdata = dtm)  # <-- agregar newdata = dtm
})
# Ver resultados
resultados_k <- tibble(k = valores_k, perplejidad = perplejidad)
# Graficar
plot(valores_k, perplejidad, type = "b", pch = 19,
xlab = "Número de tópicos (k)",
ylab = "Perplejidad",
main = "Selección de k óptimo")
# Graficar — busca el "codo" donde la perplejidad deja de bajar mucho
plot(valores_k, perplejidad, type = "b", pch = 19,
xlab = "Número de tópicos (k)",
ylab = "Perplejidad",
main = "Selección de k óptimo")
# Reemplaza k con el número que sugirió el gráfico
k <- 5  # <-- ajusta según el gráfico
modelo_lda <- LDA(
dtm,
k       = k,
method  = "Gibbs",
control = list(seed = 42, iter = 1000, burnin = 100)
)
# Top 10 palabras por tópico
palabras_topicos <- tidy(modelo_lda, matrix = "beta") %>%
group_by(topic) %>%
slice_max(beta, n = 10) %>%
ungroup() %>%
arrange(topic, -beta)
# Imprimir
palabras_topicos %>%
group_by(topic) %>%
summarise(palabras = paste(term, collapse = ", ")) %>%
print()
# Tópico dominante por frase
topicos_frases <- tidy(modelo_lda, matrix = "gamma") %>%
group_by(document) %>%
slice_max(gamma, n = 1) %>%
ungroup() %>%
mutate(frase_id = as.integer(document)) %>%
left_join(frases, by = "frase_id") %>%
select(pagina, frase_id, topico = topic, probabilidad = gamma, texto)
# Tópico dominante por página
topicos_paginas <- topicos_frases %>%
group_by(pagina, topico) %>%
summarise(n_frases = n(), .groups = "drop") %>%
group_by(pagina) %>%
slice_max(n_frases, n = 1) %>%
ungroup()
print(topicos_paginas)
View(palabras_topicos)
print(topicos_paginas)
View(topicos_frases)
